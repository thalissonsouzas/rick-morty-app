import { AkitaPlugin, Queries } from '../plugin';
import { EntityParam } from '../entity-collection-plugin';
export interface StateHistoryParams {
    maxAge?: number;
}
export declare class StateHistoryPlugin<E = any, S = any> extends AkitaPlugin<E, S> {
    protected query: Queries<E, S>;
    private params;
    private _entityId?;
    /** Allow skipping an update from outside */
    private skip;
    private history;
    /** Skip the update when redo/undo */
    private skipUpdate;
    private subscription;
    constructor(query: Queries<E, S>, params?: StateHistoryParams, _entityId?: EntityParam);
    readonly hasPast: boolean;
    readonly hasFuture: boolean;
    activate(): void;
    undo(): void;
    redo(): void;
    jumpToPast(index: number): void;
    jumpToFuture(index: number): void;
    clear(): void;
    destroy(clearHistory?: boolean): void;
    ignoreNext(): void;
    private update;
}
